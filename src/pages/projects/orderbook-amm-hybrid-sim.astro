---
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="Order-book / AMM / hybrid venue simulator — Aditya Bhosale" />
  <title>orderbook / amm / hybrid sim — Aditya Bhosale</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=EB+Garamond:ital,wght@0,400;0,500;1,400&display=swap" rel="stylesheet" />
  <style>
    :root {
    --bg: #ffffff;
    --ink: #1a1a1a;
    --ink-muted: #555555;
    --link: #1a1a1a;
    --rule: #d8d8d8;
    --cta-bg: #1a1a1a;
    --cta-bg-hover: #000000;
    --cta-fg: #ffffff;
  }

  * { box-sizing: border-box; margin: 0; padding: 0; }

  body {
    background: var(--bg);
    color: var(--ink);
    font-family: 'EB Garamond', Georgia, serif;
    font-size: 19px;
    line-height: 1.55;
    -webkit-font-smoothing: antialiased;
    padding: 60px 24px 80px;
  }

  main {
    max-width: 620px;
    margin: 0 auto;
  }

  h1 {
    font-size: 28px;
    font-weight: 500;
    margin-bottom: 6px;
    letter-spacing: -0.01em;
  }

  h1 a {
    color: var(--ink);
    text-decoration: none;
  }

  .socials {
    margin-bottom: 28px;
    color: var(--ink-muted);
    font-size: 16px;
  }

  .socials a {
    color: var(--ink-muted);
    text-decoration: none;
    margin-right: 14px;
  }

  .socials a:hover {
    color: var(--ink);
    text-decoration: underline;
  }

  p {
    margin-bottom: 18px;
  }

  a {
    color: var(--link);
  }

  a:hover {
    text-decoration: none;
  }

  .label {
    font-style: italic;
    color: var(--ink-muted);
    margin-top: 28px;
    margin-bottom: 8px;
    font-size: 17px;
  }

  /* CTA button — promoted current build link */
  .cta-wrap {
    margin: 36px 0 40px;
  }

  .cta {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    background: var(--cta-bg);
    color: var(--cta-fg);
    padding: 14px 22px;
    text-decoration: none;
    font-family: 'EB Garamond', Georgia, serif;
    font-size: 18px;
    font-weight: 500;
    letter-spacing: -0.005em;
    border-radius: 2px;
    transition: background 0.15s ease, transform 0.15s ease;
  }

  .cta:hover {
    background: var(--cta-bg-hover);
    transform: translateY(-1px);
  }

  .cta-arrow {
    font-size: 0.9em;
    line-height: 1;
  }

  .cta-sublabel {
    display: block;
    margin-top: 8px;
    font-style: italic;
    color: var(--ink-muted);
    font-size: 15px;
    line-height: 1.4;
  }

  hr {
    border: none;
    border-top: 1px solid var(--rule);
    margin: 36px 0 28px;
  }

  .footer-links {
    color: var(--ink-muted);
    font-size: 16px;
  }

  .footer-links a {
    color: var(--ink-muted);
    text-decoration: none;
    margin-right: 18px;
  }

  .footer-links a:hover {
    color: var(--ink);
    text-decoration: underline;
  }

  /* arrow superscript */
  .ext::after {
    content: " ↗︎";
    font-size: 0.85em;
  }

  @media (max-width: 600px) {
    body { padding: 40px 20px 60px; font-size: 18px; }
    h1 { font-size: 24px; }
    .cta { font-size: 17px; padding: 13px 20px; }
  }

    h1 { font-size: 26px; line-height: 1.25; }
    main p { margin-bottom: 16px; }
    .back-link {
      display: inline-block;
      margin-bottom: 24px;
      color: var(--ink-muted);
      font-size: 16px;
    }
    .links {
      margin-top: 28px;
      padding-top: 20px;
      border-top: 1px solid #d8d8d8;
    }
    .links a {
      display: inline-block;
      margin-right: 18px;
    }
  </style>
</head>
<body>
  <main>
    <a href="/projects" class="back-link">← projects</a>
    <h1>Order-book / AMM / hybrid venue simulator</h1>

    <p>Agent-based simulation framework comparing AMM, CLOB, and hybrid (CLOB + passive AMM layer) derivatives venue designs under event-driven Bayesian trader populations.</p>

    <p>Continuous-price log-linear dynamics, multi-market cluster with shared latent factor loadings, four agent classes (naive Gaussian, tail-aware base-rate priors, cross-market aggregated-evidence via cosine-similarity loadings, joint-factor information-form posteriors). 900-run sweep across 3 mechanisms × 2 population mixes × 3 capital regimes × 2 signal levels × 25 seeds.</p>

    <p>Cleanest finding: the hybrid mechanism halves agent-to-agent trade volume across every slice because the LP layer absorbs noise flow that would otherwise force informed traders into direct adverse selection. Rent flow turned out to be regime-dependent rather than mechanism-dependent — a null result against prior LS-LMSR work where capital was the binding constraint in tail regimes.</p>

    <div class="links">
      <a class="ext" href="https://github.com/adityanbhosale/orderbook-amm-hybrid-sim" target="_blank" rel="noopener">repo</a>
    </div>
  </main>
</body>
</html>